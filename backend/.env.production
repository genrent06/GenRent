# ── App ──────────────────────────────────────────────
ENV=production
APP_PORT=8080

# MUST be changed — 32+ random characters (generated securely)
JWT_SECRET=i9G4uqt8GRG1dqnuhUrbmyYc9hYFBqzOgVrhlFHzBds=

# Comma-separated allowed origins (no trailing slash)
ALLOWED_ORIGINS=https://genrent.in,https://www.genrent.in

# Base URL for email links (IMPORTANT: Update to your actual domain)
BASE_URL=https://genrent.in

# ── Database ─────────────────────────────────────────
DB_USER=genrent
DB_PASSWORD=FAHn1GAFiCxd1EFK
DB_NAME=genrent

# Internal docker connection (uses container name)
DATABASE_URL=host=postgres user=genrent password=FAHn1GAFiCxd1EFK dbname=genrent port=5432 sslmode=disable

# ── Email (Gmail SMTP) ───────────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=genrent06@gmail.com
SMTP_PASS=hlni phsk xpdw cjzq
SMTP_FROM=genrent06@gmail.com
SMTP_FROM_NAME=GenRent

# ── Payment Gateway ──────────────────────────────────
PAYMENT_GATEWAY=razorpay
# Update these with your actual Razorpay live keys
RAZORPAY_KEY_ID=rzp_live_XXXXX
RAZORPAY_KEY_SECRET=your_actual_live_secret
RAZORPAY_WEBHOOK_SECRET=your_webhook_secret

# ── Domain ───────────────────────────────────────────
# Used by Caddy for automatic TLS — must point to this server's IP
DOMAIN=genrent.in

# ── Backup ───────────────────────────────────────────
BACKUP_DIR=/var/backups/genrent
RETAIN_DAYS=7

# ── Platform Fees ───────────────────────────────────
PLATFORM_FEE_PERCENT=10.0
REFUND_AUTO_PROCESS=true
PAYMENT_TIMEOUT=900
